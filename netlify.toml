# Statický web bez build kroku.
# Nasazuje se obsah složky v2/ — starší verze zůstávají v repu jako reference.
[build]
  publish = "v2"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
